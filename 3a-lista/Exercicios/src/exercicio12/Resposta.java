/*

O método sleep() faz a thread que está sendo executada no momento parar por um
determinado número de milisegundos, passados como parâmetro no método.

O método join() faz com que a thread que está sendo executada no momento espere
outra thread finalizar a execução, para que uma thread não comece a rodar
até que outra thread termine.

*/